# 交互式图表示例 · 工作空间

VIBE 工具箱新工具「交互式图表示例」的工作空间。（文件夹名暂定 `interactive-chart`，想换名告诉我。）

## 目录约定
- `_ref/`（本目录）——**参考素材放这里**：参考图 / 示例图表截图 / 数据样例 / 想要的交互说明皆可。
  `_` 前缀会被 Jekyll 构建忽略，**不会发布上线**，纯工作用。
- `index.html` + `assets/`——最终的**自包含工具**（参照 `vibe/xiuxian/` 的打包方式），做好后放本文件夹根部；
  公开地址 `/vibe/interactive-chart/`。

## 规矩（遵循）
- 只在本文件夹内建工具，不动 `_layouts/ _includes/ _sass/ js/`。
- 参照 `vibe/xiuxian/` 的「纯 HTML + assets/，无 Jekyll front matter」自包含方式。
- 前端改动**经你审核通过才 push main**（评审门 + 先本地预览）。
- 小图 / 中间过程参考图可入库。

## 下一步
把参考素材丢进 `_ref/` 后告诉我，我看完再和你定：图表类型、技术栈（纯 JS / 某图表库）、交互方式，然后动手。
